.env
/server
/migrate
/tmp
/bin/

# Embedded frontend build (generated by make build-frontend)
# Keep .gitkeep so go:embed has at least one file before first build
internal/frontend/dist/*
!internal/frontend/dist/.gitkeep
