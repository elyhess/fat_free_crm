### Dashboard Activity Feed

**Go Backend**
- [x] GET /activity already exists (ListRecentActivity — versions across tracked entities)

**React Frontend**
- [x] Activity timeline on dashboard page (fetches GET /activity?limit=20)
- [x] Shows event type (color-coded create/update/destroy), entity type, entity ID link, user, relative timestamp
- [x] Links to entity detail pages via entity slug mapping

**Verified**
- [x] TypeScript compiles clean
